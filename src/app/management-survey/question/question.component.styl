div.title {
    padding: 7px;
    font-size: 1.2em;
    line-height: 36px;
}

div.title dx-button {
    float: right;
    margin: 0 2px;
}

div[fxflex] {
    background-color: rgba(255, 255, 255, 0.5);
}