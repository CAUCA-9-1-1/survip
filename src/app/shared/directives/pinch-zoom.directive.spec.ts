import { PinchZoomDirective } from './pinch-zoom.directive';

describe('PinchZoomDirective', () => {
  it('should create an instance', () => {
    const directive = new PinchZoomDirective();
    expect(directive).toBeTruthy();
  });
});
