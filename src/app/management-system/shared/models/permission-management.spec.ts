import { PermissionManagement } from './permission-management';

describe('PermissionManagement', () => {
  it('should create an instance', () => {
    expect(new PermissionManagement()).toBeTruthy();
  });
});
