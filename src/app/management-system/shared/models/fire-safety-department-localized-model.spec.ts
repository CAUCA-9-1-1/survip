import { FireSafetyDepartmentLocalizedModel } from './fire-safety-department-localized-model';

describe('FireSafetyDepartmentLocalizedModel', () => {
  it('should create an instance', () => {
    expect(new FireSafetyDepartmentLocalizedModel()).toBeTruthy();
  });
});
